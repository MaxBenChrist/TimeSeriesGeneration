# Autogenerated with SMOP 0.32-7-gcce8558
from smop.core import *
# MkSg_sine.m

    
@function
def MkSg_Sine(N=None,A=None,f=None,phi=None,eta=None,*args,**kwargs):
    varargin = MkSg_Sine.varargin
    nargin = MkSg_Sine.nargin

    # returns a sine wave
# Length N
# Amplitude A
# Frequency f
# Phase offset phi
# Noise amplitude eta
    
    #-------------------------------------------------------------------------------
## Set defaults
    if nargin < 1 or isempty(N):
        N=1000
    
    if nargin < 2 or isempty(A):
        A=1
    
    if nargin < 3 or isempty(f):
        f=1 / 20
    
    if nargin < 4 or isempty(phi):
        phi=0
    
    if nargin < 5 or isempty(eta):
        eta=0
    
    #-------------------------------------------------------------------------------
## Make the signal:
    s=dot(A,sin(dot(dot(dot(2,pi),f),(arange(1,N)).T) + phi)) + dot(eta,randn(N,1))
    return s
    
if __name__ == '__main__':
    pass
    